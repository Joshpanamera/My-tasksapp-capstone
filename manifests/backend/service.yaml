apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: taskapp
  labels:
    app: backend
    tier: api

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
